# 更新日志

## v1.2.0

- 视频详情从被风控的 `/x/web-interface/view` 切换至匿名可用的 `/x/web-interface/wbi/view`
- 使用 `curl_cffi` 模拟 Chrome 的 TLS/JA3、HTTP/2 与请求头指纹，修复同一 IP 下浏览器正常但脚本请求 HTTP 412 的问题
- 完全移除 `SESSDATA` 和登录 Cookie 配置，并丢弃服务端下发的匿名 Cookie
- 保留请求限速、412 冷却熔断和完整视频简介

## v1.1.0

- 修复 B 站 HTTP 412 风控后持续重复请求的问题
- 新增可选 `SESSDATA` 登录态配置
- 首次解析前初始化匿名 Cookie，并补充浏览器请求头
- 新增全局请求限速和可配置的 412 冷却熔断
- 视频简介不再截断，所有排版风格和 `${简介}` 均显示完整内容
- 对外隐藏内部异常详情，完整堆栈仅写入日志

## v1.0.3

- 新增视频解析排版风格选择：原始格式 / 简洁风格 / 自定义
- 自定义模式支持变量：`${标题}` `${UP主}` `${UP主链接}` `${简介}` `${封面}` `${点赞}` `${投币}` `${收藏}` `${转发}` `${观看}` `${弹幕数量}` `${评论}` `${链接}` `${发布时间}` `${类型}` `${BV号}` `${时长}` `${版权}`

**原始格式**（默认）：

```
https://www.bilibili.com/video/BVxxxxxxxxxx

标题："视频标题"
类型：分区 | UP：UP主名称 | https://space.bilibili.com/xxx

播放：xxx万 | 弹幕：xxx | 收藏：xxx
点赞：xxx | 硬币：xxx | 评论：xxx

简介：-
```

**简洁风格**：

```
🎬 标题：视频标题
👤 UP主：UP主名称
📝 简介：-
[封面图]
👍 点赞：xxx 🪙 投币：xxx
❤️ 收藏：xxx 🔄 转发：xxx
👀 观看：xxx 💬 弹幕：xxx
```

## v1.0.2

- 更新插件介绍
- 修复多段文字被拆成多条消息发送的问题
